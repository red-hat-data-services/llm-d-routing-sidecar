apiVersion: v1
kind: Service
metadata:
  name: qwen-decoder
spec:
  selector:
    app: qwen-decoder
  clusterIP: None
  ports:
    - name: nixl
      port: 55555
      protocol: TCP
    - name: vllm
      port: 8000
      protocol: TCP
